{
  "name": "tspathtestcore",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest --runInBand",
    "watch": "rm -rf dist && tsc -w -p tsconfig-build.json & tsc-alias -w -p tsconfig-build.json",
    "build": "rm -rf dist && tsc -p tsconfig-build.json && tsc-alias -p tsconfig-build.json",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "@types/node": "^17.0.22",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.7.0",
    "tsc-alias": "^1.6.4",
    "typescript": "^4.6.2"
  }
}
